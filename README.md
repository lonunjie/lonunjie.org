# lonunjie.org

Website for [SeedLife](https://lonunjie.org) — *Protect life. Seed it.*

Static HTML/CSS in `public/`, served by Cloudflare Pages. No build step:
open `public/index.html` or run `python3 -m http.server -d public`.

Contributions to improve the site are welcome.
